log:
  level: debug
  format: text

rest:
  port: 8001

postgres:
  dsn: "host=postgres user=admin password=admin dbname=opsway port=5432 sslmode=disable"
  debug: true

authentication:
  secret: "secret"
  audience: "https://opsway.io"
  issuer: "opsway.io"
  expires_in: 24h
  refresh_expires_in: 720h 

clickhouse:
  dsn: "host=clickhouse user=default password= dbname=opsway port=8123 sslmode=disable"
  debug: true

asynq:
  host: localhost
  port: 6379